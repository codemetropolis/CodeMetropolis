## Tags
#pullRequest_199 #component_rendering #type_feature #category_output #testFile-format_XML #status_task_under_testing 

# [Test] Issue no. #199's solution if no warning appears when rendering ran on normal input

_Related issue:_ https://github.com/codemetropolis/CodeMetropolis/issues/199
_Related branch:_ https://github.com/codemetropolis/CodeMetropolis/tree/199-rendering-with-invalid-inputfile-is-not-handled-correctly

**Purpose**: The purpose of this test is to see if the solution committed to issue no. #199 works as intended (when rendering module is ran with a valid input file) and creates a normal output "world" folder as a result.

**Pre-requisites**:		
- Java runtime 1.8
- Command line opened in the folder of rendering-1.4.0.jar
- A *placingToRendering.xml* file exists in the rendering-1.4.0.jar's folder. (Has to be a valid file, which was produced by running placing-1.4.0.jar with the *mappingToPlacing.xml* as input, which was generated by mapping-1.4.0.jar with the *cdfFile* generated by converter-1.4.0.jar with codemetropolis-toolchain-commons.graph as input)

**Steps**:
1. Run `java -jar rendering-1.4.0.jar -i placingToRendering.xml -w world` in rendering-1.4.0.jar command line

_Expected result:_
The tool works as intended on a valid input file and creates a world folder as output when finishes running.
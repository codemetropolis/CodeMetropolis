## Tags
#pullRequest_195 #component_placing #type_bug #category_input #status_task_under_testing

# [Test] Check issue no. #195's solution if it returns an error when an invalid layout parameter is used

_Related issue:_ https://github.com/codemetropolis/CodeMetropolis/issues/195
_Related branch:_ https://github.com/codemetropolis/CodeMetropolis/tree/195-placing-with-empty-layout-parameter-is-not-handled-correctly

**Purpose**: The purpose of this test is to check if the issue's solution still prevents the use of invalid layout parameters and gives back an error message.

**Pre-requisites**:		
- Java runtime 1.8
- Command line opened in the folder of placing-1.4.0.jar
- A *mappingToPlacing.xml* file exists in the placing-1.4.0.jar's folder. (Has to be a valid file, which was produced by running converter-1.4.0.jar with the *converterToMapping.xml* as input)

**Steps**:
1.  Run `java -jar placing-1.4.0.jar -i mappingToPlacing.xml -l ASD` in rendering-1.4.0.jar command line

_Expected result:_
The placing module finishes running and an error message is given back regarding the invalid layout parameter.